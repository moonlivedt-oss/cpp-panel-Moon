# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor / IDE
.vscode-test/

# Логи и временное
*.log
*.tmp

# Node (на случай, если появятся зависимости)
node_modules/

# Собранные пакеты держим локально: ставятся командой, в истории не нужны
dist/*.vsix

# Генерируемые превью (npm run preview / preview:window) — собираются из исходников
build/

# Кэш линтера ruff (проверка docs/proverka.py) — чисто локальный
.ruff_cache/

# Личные разовые инструменты и бэкапы (пути захардкожены под конкретную машину,
# в репозитории им не место): ремонт вставки в workbench и его бэкап.
fix-window.js
workbench.backup.html
