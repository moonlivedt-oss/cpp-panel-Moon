# Тема 6. Контейнеры: vector, map, set, pair

Контейнеры — это готовые «умные хранилища» стандартной библиотеки. Вместо того чтобы
вручную городить массивы и счётчики, вы берёте `vector` (список), `map` (словарь
«ключ → значение»), `set` (множество уникальных) — и решаете задачу в несколько строк.
Это водораздел между «пишу как на паскале» и «пишу на C++».

> **Теория темы.** Опирается на разделы 11–15 справочника ([открыть теорию](../ref/06-konteynery.md)).
> Там про `vector`, `map` как счётчик, `set`, `pair` и типичные ловушки (`m[key]` создаёт ключ!).

> **В целых программах.** «Анализ текста» (`../examples/03-analiz-teksta.md`) — `map`-счётчик
> и топ по частоте; «Список дел» (`../examples/02-spisok-del.md`) — `vector` структур;
> «Телефонная книга» (`../examples/06-telefonnaya-kniga.md`) — `map` с сохранением между запусками.

[← К списку тем и пути изучения](00-README.md)

---

## Задачи темы

| № | | Задача | Что тренирует |
|---|---|---|---|
| 6.1 | 🟢 | Массив наоборот | `vector`, `reverse` |
| 6.2 | 🟢 | Сколько различных | `set` |
| 6.3 | 🟡 | Самое частое слово | `map`-счётчик |
| 6.4 | 🟡 | Ведомость оценок | массив-счётчик |
| 6.5 | 🟡 | Победитель по очкам | максимум по полю |
| 6.6 | 🔴 | Первый неповторяющийся | частоты + порядок |
| 6.7 | 🟡 | Суммы по категориям | `map`-аккумулятор |
| 6.8 | 🔴 | Пересечение множеств | два `set` |

<sub>🟢 всё нужное уже знаете · 🟡 один новый приём · 🔴 нужно подумать · [полный указатель задач](10-spisok-zadach.md)</sub>

---

## Чему вы здесь научитесь

- Хранить список значений в `vector` и обходить его.
- Считать частоты через `map` (`++freq[key]`) — приём номер один в обработке данных.
- Находить количество уникальных через `set`.
- Понимать, что `map` хранит ключи **отсортированными** — и как это использовать.

## Где это пригодится

`map`-счётчик — это буквально «сколько раз встретилось» для чего угодно: слова в
тексте, коды ошибок в логе, товары в заказах, голоса на выборах. `vector` — самый
частый контейнер вообще. `set` — «убрать дубликаты» и «сколько разных». Эти три
структуры покрывают огромную долю реальных задач обработки данных.

---

## 6.1. Массив наоборот 🟢

*`vector` — рабочая лошадка C++: сложили числа в список, а потом прошли его с конца.*

Дано `N` чисел. Напечатайте их в обратном порядке в одну строку через пробел.

**Формат ввода.** `N` (0 ≤ N ≤ 1000), затем `N` целых чисел.

**Формат вывода.** Те же числа в обратном порядке, через пробел.

**Пример:**
```
Ввод
5
1 2 3 4 5
Вывод
5 4 3 2 1
```

- **Тренирует:** заполнение `vector` и обход с конца.
- **Где пригодится:** «последние сначала» (лента новостей, история), базовая работа со списком.

> **Как подступиться.** Считайте в `vector`, затем печатайте от последнего индекса к нулевому.

Написал решение — прогони его на этих входах (нажми «копировать», вставь в программу), особенно на краях:

```tests
# обычные (первое число — сколько дальше чисел)
5 1 2 3 4 5 => 5 4 3 2 1
3 -1 0 5 => 5 0 -1
# край: одно число; а при N=0 вывод пустой
1 42 => 42
```

<details>
<summary>Полное решение с разбором — открывай, только если застрял</summary>

Ход мысли по шагам:

1. Читаю числа в `std::vector` через `push_back`.
2. Переворачиваю вектор готовым `std::reverse` из `<algorithm>`.
3. Печатаю через пробел; флаг `first` убирает лишний пробел перед первым числом.

```cpp
#include <iostream>
#include <vector>
#include <algorithm>

int main() {
    int n = 0;
    std::cin >> n;

    std::vector<int> v;
    for (int i = 0; i < n; ++i) {
        int x = 0;
        std::cin >> x;
        v.push_back(x);
    }

    std::reverse(v.begin(), v.end());

    bool first = true;
    for (int x : v) {
        if (!first) std::cout << " ";
        std::cout << x;
        first = false;
    }
    std::cout << "\n";
    return 0;
}
```

Открывай это, только когда правда застрял — весь смысл в том, чтобы пройти путь «затык → идея → код» самому.
</details>

---

## 6.2. Сколько различных 🟢

*Бросьте все числа в `set` — он молча выкинет повторы. Сколько осталось, столько и уникальных.*

Дано `N` чисел. Напечатайте, сколько среди них **различных**.

**Формат ввода.** `N` (0 ≤ N ≤ 100000), затем `N` целых чисел.

**Формат вывода.** Количество различных значений.

**Пример:**
```
Ввод
6
3 1 3 2 1 3
Вывод
3
```

- **Тренирует:** `set` как хранилище уникальных значений.
- **Где пригодится:** «сколько уникальных посетителей», «сколько разных товаров», дедупликация.

> **Как подступиться.** Сложите все числа в `set` — он сам отбросит повторы; ответ — его размер.

Написал решение — прогони его на этих входах (нажми «копировать», вставь в программу), особенно на краях:

```tests
# пустой ввод (N=0); все одинаковые
5 1 2 2 3 1 => 3
0 => 0
4 7 7 7 7 => 1
```

<details>
<summary>Полное решение с разбором — открывай, только если застрял</summary>

Ход мысли по шагам:

1. Складываю все числа в `std::set` — он молча выбрасывает повторы.
2. Значит, различных ровно столько, каков размер множества.
3. Печатаю `set.size()`.

```cpp
#include <iostream>
#include <set>

int main() {
    int n = 0;
    std::cin >> n;

    std::set<int> seen;
    for (int i = 0; i < n; ++i) {
        int x = 0;
        std::cin >> x;
        seen.insert(x);        // повторы set отбросит сам
    }

    std::cout << seen.size() << "\n";
    return 0;
}
```

Открывай это, только когда правда застрял — весь смысл в том, чтобы пройти путь «затык → идея → код» самому.
</details>

---

## 6.3. Самое частое слово 🟡

*`map`-счётчик — приём номер один в обработке данных. А ключи в нём лежат по алфавиту, так что ничья решается сама.*

Дано `N` слов. Напечатайте слово, которое встречается чаще всего. Если таких
несколько, напечатайте наименьшее по алфавиту.

**Формат ввода.** `N` (1 ≤ N ≤ 100000), затем `N` слов (латиница, без пробелов).

**Формат вывода.** Одно слово — самое частое.

**Пример:**
```
Ввод
5
apple pear apple pear banana
Вывод
apple
```

- **Тренирует:** `map` как счётчик частот и поиск максимума с учётом правила при равенстве.
- **Где пригодится:** топ-запросы, самый популярный товар, анализ логов, «горячие» ключи.

> **Как подступиться.** Посчитайте частоты в `map`; `map` отсортирован по ключу, поэтому при обходе первым запомнится алфавитно меньшее.

> **Разбор примера.** `apple` и `pear` встречаются по 2 раза; `apple` меньше по алфавиту, поэтому он.

Написал решение — прогони его на этих входах (нажми «копировать», вставь в программу), особенно на краях:

```tests
# самое частое слово
5 a b a c a => a
3 x y y => y
```

<details>
<summary>Подсказка посильнее — с чего начать</summary>

Частоты — в `map<string,int>`; перебирая его (он по ключу), строгим `>` возьмёшь меньшее слово при равенстве.

```c++
std::map<std::string, int> freq;
// в цикле: ++freq[word];
std::string answer;  int best = 0;
for (const auto &[word, c] : freq)
    if (c > best) { best = c; answer = word; }
```

</details>

<details>
<summary>Полное решение с разбором — открывай, только если застрял</summary>

Ход мысли по шагам:

1. Считаю частоты в `std::map<string,int>`: `++freq[word]`.
2. `map` хранит ключи по алфавиту, поэтому обхожу его по порядку.
3. Запоминаю слово со **строго** большим счётчиком — при равенстве первым останется алфавитно меньшее.

```cpp
#include <iostream>
#include <string>
#include <map>

int main() {
    int n = 0;
    std::cin >> n;

    std::map<std::string, int> freq;
    for (int i = 0; i < n; ++i) {
        std::string word;
        std::cin >> word;
        ++freq[word];
    }

    std::string answer;
    int best = 0;
    for (const auto &[word, count] : freq) {   // map отсортирован по ключу
        if (count > best) {                    // строгое > — при равенстве меньшее слово
            best = count;
            answer = word;
        }
    }

    std::cout << answer << "\n";
    return 0;
}
```

Открывай это, только когда правда застрял — весь смысл в том, чтобы пройти путь «затык → идея → код» самому.
</details>

---

## 6.4. Ведомость оценок 🟡

*Оценки всего от 2 до 5 — значит, счётчик поместится в обычный маленький массив по индексу-оценке.*

Дано `N` оценок (числа от 2 до 5). Напечатайте, сколько раз встретилась каждая оценка
— по возрастанию оценки.

**Формат ввода.** `N` (0 ≤ N ≤ 100000), затем `N` чисел от 2 до 5.

**Формат вывода.** По одной строке на каждую оценку от 2 до 5 в формате `оценка: количество`. Печатайте все четыре оценки, даже если какой-то не было (тогда количество `0`).

**Пример:**
```
Ввод
6
5 4 5 3 5 4
Вывод
2: 0
3: 1
4: 2
5: 3
```

- **Тренирует:** массив-счётчик по фиксированному диапазону значений.
- **Где пригодится:** гистограммы, распределения (звёзды рейтинга, возрастные группы), подсчёт по категориям.

> **Как подступиться.** Заведите счётчик на индексы 2..5, увеличивайте по оценке, потом выведите все четыре — включая нулевые.

<details>
<summary>Подсказка посильнее — с чего начать</summary>

Массив счётчиков по оценкам 2..5, потом печатай все четыре — даже нулевые.

```c++
int cnt[6] = {};              // используем индексы 2..5
// в цикле: ++cnt[grade];
for (int g = 2; g <= 5; ++g)
    std::cout << g << ": " << cnt[g] << "\n";
```

</details>

<details>
<summary>Полное решение с разбором — открывай, только если застрял</summary>

Ход мысли по шагам:

1. Оценки только 2..5 — беру массив-счётчик и увеличиваю `cnt[grade]`.
2. Печатаю все четыре оценки от 2 до 5 в цикле.
3. Даже если оценки не было, её счётчик `0` — так и выведется (условие требует все четыре).

```cpp
#include <iostream>

int main() {
    int n = 0;
    std::cin >> n;

    int cnt[6] = {};               // индексы 2..5 нам и нужны
    for (int i = 0; i < n; ++i) {
        int grade = 0;
        std::cin >> grade;
        ++cnt[grade];
    }

    for (int g = 2; g <= 5; ++g)
        std::cout << g << ": " << cnt[g] << "\n";
    return 0;
}
```

Открывай это, только когда правда застрял — весь смысл в том, чтобы пройти путь «затык → идея → код» самому.
</details>

---

## 6.5. Победитель по очкам 🟡

*Чтобы найти чемпиона по очкам, контейнер не нужен: держите лучшее имя и лучший счёт, обновляйте на ходу.*

Даны `N` записей вида «имя очки». Напечатайте имя участника с наибольшим числом
очков. Гарантируется, что победитель один.

**Формат ввода.** `N` (1 ≤ N ≤ 100000), затем `N` пар: строка-имя и целое число очков.

**Формат вывода.** Имя победителя.

**Пример:**
```
Ввод
3
anna 40
boris 55
vera 33
Вывод
boris
```

- **Тренирует:** параллельное чтение пары «строка + число» и поиск максимума по одному полю.
- **Где пригодится:** «кто набрал больше всех», лидерборд, поиск рекордсмена в потоке данных.

> **Как подступиться.** Можно вообще без контейнера — держите `bestName` и `bestScore`, обновляйте при большем счёте.

Написал решение — прогони его на этих входах (нажми «копировать», вставь в программу), особенно на краях:

```tests
# один участник
3 anna 5 bob 8 cara 3 => bob
1 solo 42 => solo
```

<details>
<summary>Подсказка посильнее — с чего начать</summary>

Читай пары «имя очки», держи лучшего; старт — очень маленькое число.

```c++
#include <limits>
std::string bestName;
int bestScore = std::numeric_limits<int>::min();
// в цикле: cin >> name >> score; if (score > bestScore) { bestScore = score; bestName = name; }
```

</details>

<details>
<summary>Полное решение с разбором — открывай, только если застрял</summary>

Ход мысли по шагам:

1. Контейнер не нужен: держу `bestName` и `bestScore`.
2. `bestScore` стартует с минимально возможного `int`, чтобы отрицательные очки не сломали сравнение.
3. Читаю пары «имя очки» и обновляю лидера, когда очки больше текущего максимума.

```cpp
#include <iostream>
#include <string>
#include <limits>

int main() {
    int n = 0;
    std::cin >> n;

    std::string bestName;
    int bestScore = std::numeric_limits<int>::min();   // старт — очень маленькое

    for (int i = 0; i < n; ++i) {
        std::string name;
        int score = 0;
        std::cin >> name >> score;
        if (score > bestScore) {
            bestScore = score;
            bestName = name;
        }
    }

    std::cout << bestName << "\n";
    return 0;
}
```

Открывай это, только когда правда застрял — весь смысл в том, чтобы пройти путь «затык → идея → код» самому.
</details>

---

## 6.6. Первый неповторяющийся 🔴

*Кто встретился ровно один раз и раньше всех? Частоты подскажет `map`, а порядок сохранит `vector` — ответ на их стыке.*

Дано `N` чисел. Напечатайте **первое** (в порядке ввода) число, которое встречается
ровно один раз. Если такого нет — напечатайте `-1`.

**Формат ввода.** `N` (0 ≤ N ≤ 100000), затем `N` целых чисел.

**Формат вывода.** Первое неповторяющееся число или `-1`.

**Пример:**
```
Ввод
6
4 5 4 6 5 7
Вывод
6
```

- **Тренирует:** совмещение двух проходов — сначала частоты (`map`), затем поиск в **исходном** порядке.
- **Где пригодится:** «первый уникальный запрос», обнаружение единичных событий, дедупликация с сохранением порядка.

> **Как подступиться.** Сохраните числа в `vector` и параллельно посчитайте частоты в `map`. Затем пройдите по `vector` (именно по нему — он хранит порядок ввода) и верните первое число с частотой 1.

> **Разбор примера.** В примере `4` и `5` повторяются, `6` — первое встретившееся с единственным вхождением.

Написал решение — прогони его на этих входах (нажми «копировать», вставь в программу), особенно на краях:

```tests
# если неповторяющихся нет — минус один
5 1 2 2 3 1 => 3
3 7 7 7 => -1
4 5 5 6 6 => -1
```

<details>
<summary>Подсказка посильнее — с чего начать</summary>

Числа — в `vector` (хранит порядок), частоты — в `map`. Потом иди по vector и верни первое с частотой 1.

```c++
std::vector<int> v;  std::map<int,int> cnt;
// в цикле: v.push_back(x); ++cnt[x];
int answer = -1;
for (int x : v) if (cnt[x] == 1) { answer = x; break; }
```

</details>

<details>
<summary>Полное решение с разбором — открывай, только если застрял</summary>

Ход мысли по шагам:

1. Первый проход: сохраняю числа в `vector` (он хранит порядок ввода) и заодно считаю частоты в `map`.
2. Второй проход — по `vector`: возвращаю первое число с частотой 1.
3. Если такого нет — печатаю `-1`. По `map` идти нельзя: он отсортирован, порядок ввода в нём потерян.

```cpp
#include <iostream>
#include <vector>
#include <map>

int main() {
    int n = 0;
    std::cin >> n;

    std::vector<int> v;              // хранит порядок ввода
    std::map<int, int> cnt;          // хранит частоты
    for (int i = 0; i < n; ++i) {
        int x = 0;
        std::cin >> x;
        v.push_back(x);
        ++cnt[x];
    }

    int answer = -1;
    for (int x : v) {                // идём именно по vector — по порядку
        if (cnt[x] == 1) {
            answer = x;
            break;
        }
    }

    std::cout << answer << "\n";
    return 0;
}
```

Открывай это, только когда правда застрял — весь смысл в том, чтобы пройти путь «затык → идея → код» самому.
</details>

---

## 6.7. Суммы по категориям 🟡

*`total[категория] += сумма` — и `map` сам сложит всё по группам. Так рождается любой отчёт «итого по…».*

Даны `N` записей вида «категория сумма» (категория — слово). Для каждой категории
напечатайте её суммарное значение, по алфавиту категорий.

**Формат ввода.** `N` (1 ≤ N ≤ 100000), затем `N` пар: слово-категория и целое число.

**Формат вывода.** По строке на категорию (в алфавитном порядке): `категория сумма`.

**Пример:**
```
Ввод
4
food 100
transport 50
food 200
food 30
Вывод
food 330
transport 50
```

- **Тренирует:** `map` как аккумулятор по ключу (`total[key] += value`).
- **Где пригодится:** агрегация расходов, суммы продаж по товарам, любые отчёты «итого по группе».

> **Как подступиться.** `map<string, long long>`; прибавляйте сумму к нужному ключу; обойдите `map` в конце.

<details>
<summary>Подсказка посильнее — с чего начать</summary>

`map<string,long long>` сам создаёт ключ и уже отсортирован по алфавиту — то, что нужно.

```c++
std::map<std::string, long long> total;
// в цикле: cin >> cat >> amount; total[cat] += amount;
for (const auto &[cat, sum] : total)
    std::cout << cat << " " << sum << "\n";
```

</details>

<details>
<summary>Полное решение с разбором — открывай, только если застрял</summary>

Ход мысли по шагам:

1. Беру `std::map<string, long long>` — аккумулятор по ключу.
2. `total[cat] += amount;` — при первом обращении ключ создаётся сам со значением 0.
3. В конце обхожу `map` (он уже по алфавиту) и печатаю пары.

```cpp
#include <iostream>
#include <string>
#include <map>

int main() {
    int n = 0;
    std::cin >> n;

    std::map<std::string, long long> total;
    for (int i = 0; i < n; ++i) {
        std::string cat;
        long long amount = 0;
        std::cin >> cat >> amount;
        total[cat] += amount;        // ключ создастся сам, если его не было
    }

    for (const auto &[cat, sum] : total)   // map уже по алфавиту
        std::cout << cat << " " << sum << "\n";
    return 0;
}
```

Открывай это, только когда правда застрял — весь смысл в том, чтобы пройти путь «затык → идея → код» самому.
</details>

---

## 6.8. Пересечение множеств 🔴

*Сложите оба набора в `set` — дубликаты уйдут — и просто спросите, кто из первого встречается во втором.*

Даны два набора чисел. Напечатайте, сколько **различных** чисел встречается в обоих
наборах одновременно.

**Формат ввода.** `N`, затем `N` чисел; затем `M`, затем `M` чисел.

**Формат вывода.** Количество различных общих чисел.

**Пример:**
```
Ввод
3
1 2 3
4
2 3 4 5
Вывод
2
```

- **Тренирует:** два `set` и проверку принадлежности одного в другом.
- **Где пригодится:** общие элементы (общие друзья, совпадающие теги, пересечение прав доступа).

> **Как подступиться.** Оба набора — в `set` (это сразу убирает дубликаты); пройдите по первому и считайте тех, кто есть во втором.

> **Разбор примера.** Общие числа: `2` и `3` — их два.

Написал решение — прогони его на этих входах (нажми «копировать», вставь в программу), особенно на краях:

```tests
# считаются только различные общие числа; и случай без пересечения
3 1 2 3 3 2 3 4 => 2
2 1 2 2 3 4 => 0
```

<details>
<summary>Подсказка посильнее — с чего начать</summary>

Оба набора — в `set` (убирает дубликаты); пройди по первому и считай тех, кто есть во втором.

```c++
std::set<int> a, b;   // заполнить из N и M чисел
int count = 0;
for (int x : a) if (b.count(x)) ++count;
```

</details>

<details>
<summary>Полное решение с разбором — открывай, только если застрял</summary>

Ход мысли по шагам:

1. Оба набора складываю в `set` — дубликаты уходят сами.
2. Прохожу по первому множеству и считаю тех, кто есть во втором (`b.count(x)`).
3. Печатаю счётчик — число различных общих чисел.

```cpp
#include <iostream>
#include <set>

int main() {
    int n = 0;
    std::cin >> n;
    std::set<int> a;
    for (int i = 0; i < n; ++i) {
        int x = 0;
        std::cin >> x;
        a.insert(x);
    }

    int m = 0;
    std::cin >> m;
    std::set<int> b;
    for (int i = 0; i < m; ++i) {
        int x = 0;
        std::cin >> x;
        b.insert(x);
    }

    int count = 0;
    for (int x : a)
        if (b.count(x)) ++count;     // есть ли x во втором наборе
    std::cout << count << "\n";
    return 0;
}
```

Открывай это, только когда правда застрял — весь смысл в том, чтобы пройти путь «затык → идея → код» самому.
</details>

---

# Подсказки

> **Мало подсказки?** Откройте теорию темы: [раздел справочника](../ref/06-konteynery.md).

<details>
<summary>6.1 — Массив наоборот</summary>

- Считайте в `std::vector<int> v(N);` циклом `for (int i = 0; i < N; ++i) std::cin >> v[i];`.
- Печатайте с конца: `for (int i = v.size() - 1; i >= 0; --i)`. Индекс держите в `int` (см. тему «Строки»).
</details>

<details>
<summary>6.2 — Сколько различных</summary>

- `std::set<int>` хранит только уникальные значения. Добавляйте каждое число: `s.insert(x);`.
- Ответ — это `s.size()`.
</details>

<details>
<summary>6.3 — Самое частое слово</summary>

- Считайте частоты в `std::map<std::string, int> freq;` — `++freq[word];`.
- Пройдите по `freq` (он отсортирован по ключу!) и запомните слово с максимальным счётчиком. Идёте по возрастанию алфавита, поэтому при равенстве первым запомнится наименьшее — как раз нужно.
- Обновляйте только при **строго большем** счётчике: `if (count > best) { best = count; answer = word; }`.
</details>

<details>
<summary>6.4 — Ведомость оценок</summary>

- Проще всего массив-счётчик: `int cnt[6] = {0};` и `++cnt[grade];` (индексы 2..5 заняты).
- Затем цикл `for (int g = 2; g <= 5; ++g) std::cout << g << ": " << cnt[g] << "\n";`.
- Печатаете все оценки от 2 до 5, даже нулевые — так требует условие.
</details>

<details>
<summary>6.5 — Победитель по очкам</summary>

- Задачу можно решить и без контейнера: читайте пары и держите `bestName`, `bestScore`, обновляя их, когда очки больше текущего максимума.
- Инициализируйте `bestScore` очень маленьким числом (или значением первой записи), чтобы отрицательные очки не сломали сравнение.
</details>

<details>
<summary>6.6 — Первый неповторяющийся</summary>

- Первый проход: `std::vector<int> v(N);` читаете и заодно `++cnt[x];`, где `cnt` — `std::map<int,int>` (или `unordered_map`).
- Второй проход — по `v`: `for (int x : v) if (cnt[x] == 1) { ответ = x; break; }`.
- Порядок ввода хранит именно `vector`; по `map` идти нельзя — он отсортирован. Если подходящего нет — выведите `-1`.
</details>

<details>
<summary>6.7 — Суммы по категориям</summary>

- `std::map<std::string, long long> total;`.
- В цикле: `total[cat] += amount;` — при первом обращении ключ создастся сам со значением 0.
- В конце обойдите `map` (он по алфавиту): `for (const auto &[cat, sum] : total) ...`.
</details>

<details>
<summary>6.8 — Пересечение множеств</summary>

- Два `std::set<int> a, b;` — заполните из двух наборов (дубликаты уйдут сами).
- Пройдите по `a` и считайте: `for (int x : a) if (b.count(x)) ++count;`.
</details>

---

## Проверьте себя (мини тест-план)

- **6.1:** `N = 0` — пустой вывод; `N = 1` — одно число.
- **6.2:** все числа одинаковые (`1`); все разные; `N = 0` → `0`.
- **6.3:** все слова разные (ответ — алфавитно первое); одно слово повторяется.
- **6.4:** какой-то оценки нет вовсе (должна вывестись с `0`); `N = 0` — все нули.
- **6.5:** отрицательные очки у всех — вот где ломается `bestScore = 0`; один участник.
- **6.6:** все числа повторяются (`-1`); все уникальны (первое из ввода); `N = 0` → `-1`.
- **6.7:** одна категория; несколько категорий; проверьте суммирование повторов одной категории.
- **6.8:** нет общих (`0`); все общие; дубликаты во втором наборе (не считаются дважды).

**Тема закрыта,** когда `map`-счётчик вы пишете на автомате, а под задачу сразу выбираете `vector`/`map`/`set`.

---

[← Строки](05-stroki.md) · [К списку тем](00-README.md) · [Алгоритмы →](07-algoritmy.md)
