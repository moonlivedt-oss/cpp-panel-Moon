# Тема 7. Итераторы и алгоритмы

Стандартная библиотека умеет сортировать, искать, считать и фильтровать — вам не нужно
писать это руками. Здесь вы научитесь пользоваться `std::sort`, `count_if`,
`min/max_element` и задавать своё правило через лямбду. Это переход от «я пишу циклы»
к «я комбинирую готовые кубики» — так пишут в реальных проектах.

> **Теория темы.** Опирается на разделы 17–18 справочника ([открыть теорию](../ref/07-algoritmy.md)).
> Там про `begin()/end()`, `sort`, `count_if`, `find_if`, лямбды и типичные ловушки (`*max_element`, `0LL`).

> **В целой программе.** «Журнал оценок» (`../examples/08-zhurnal-ocenok.md`) — сортировка
> структур по полю и вывод лучших.

[← К списку тем и пути изучения](00-README.md)

---

## Задачи темы

| № | | Задача | Что тренирует |
|---|---|---|---|
| 7.1 | 🟢 | По возрастанию | `sort` |
| 7.2 | 🟡 | По убыванию | лямбда-компаратор |
| 7.3 | 🔴 | Сколько больше среднего | `accumulate` + `count_if` |
| 7.4 | 🟡 | Медиана | `sort` + центр |
| 7.5 | 🔴 | Топ-3 по баллам | сортировка структур |
| 7.6 | 🟢 | Минимум и максимум | `min/max_element` |
| 7.7 | 🟡 | Сколько чётных | `count_if` |
| 7.8 | 🔴 | Слова по длине | сортировка по ключу |

<sub>🟢 всё нужное уже знаете · 🟡 один новый приём · 🔴 нужно подумать · [полный указатель задач](10-spisok-zadach.md)</sub>

---

## Чему вы здесь научитесь

- Сортировать `vector` по возрастанию и по своему правилу (лямбда-компаратор).
- Считать элементы по условию (`count_if`) и суммировать (`accumulate`).
- Захватывать переменные в лямбду (`[avg]`).
- Сортировать вектор структур по нескольким полям сразу.

## Где это пригодится

Сортировка и фильтрация — хлеб любой обработки данных: топ-N результатов, поиск
подходящих записей, ранжирование. Лямбды и алгоритмы делают код короче и надёжнее
ручных циклов (меньше места для ошибки на единицу). Правило «сортировать по нескольким
критериям» встречается в любой таблице с сортировкой по колонкам.

---

## 7.1. Отсортировать по возрастанию 🟢

*Один вызов `sort` — и дальше открыты медиана, бинарный поиск, дедупликация. Сортировка почти всегда идёт первой.*

Дано `N` чисел. Напечатайте их по возрастанию в одну строку через пробел.

**Формат ввода.** `N` (0 ≤ N ≤ 100000), затем `N` целых чисел.

**Формат вывода.** Числа по возрастанию через пробел.

**Пример:**
```
Ввод
5
3 1 4 1 5
Вывод
1 1 3 4 5
```

- **Тренирует:** базовый вызов `std::sort` над `vector`.
- **Где пригодится:** любой упорядоченный вывод; сортировка — предподготовка для медианы, бинарного поиска, дедупликации.

> **Как подступиться.** `std::sort(v.begin(), v.end());` — и печатайте.

Написал решение — прогони его на этих входах (нажми «копировать», вставь в программу), особенно на краях:

```tests
# обычные (первое число — сколько дальше чисел)
5 3 1 4 1 5 => 1 1 3 4 5
3 -5 -1 -3 => -5 -3 -1
# края
1 42 => 42
4 9 9 9 9 => 9 9 9 9
```

<details>
<summary>Полное решение с разбором — открывай, только если застрял</summary>

Ход мысли по шагам:

1. Читаю числа в `std::vector`.
2. Сортирую готовым `std::sort(v.begin(), v.end())` — вручную сортировку писать не нужно.
3. Печатаю через пробел; флаг `first` — чтобы не было пробела в начале.

```cpp
#include <iostream>
#include <vector>
#include <algorithm>

int main() {
    int n = 0;
    std::cin >> n;

    std::vector<int> v;
    for (int i = 0; i < n; ++i) {
        int x = 0;
        std::cin >> x;
        v.push_back(x);
    }

    std::sort(v.begin(), v.end());

    bool first = true;
    for (int x : v) {
        if (!first) std::cout << " ";
        std::cout << x;
        first = false;
    }
    std::cout << "\n";
    return 0;
}
```

Открывай это, только когда правда застрял — весь смысл в том, чтобы пройти путь «затык → идея → код» самому.
</details>

---

## 7.2. По убыванию 🟡

*Хотите порядок наоборот? Подскажите `sort`, кто раньше, лямбдой `a > b` — и получите любой свой порядок.*

То же, что и предыдущая, но по **убыванию**. Используйте `std::sort` с собственным
правилом сравнения (лямбдой).

**Формат ввода.** `N` (0 ≤ N ≤ 100000), затем `N` целых чисел.

**Формат вывода.** Числа по убыванию через пробел.

**Пример:**
```
Ввод
5
3 1 4 1 5
Вывод
5 4 3 1 1
```

- **Тренирует:** лямбда-компаратор — своё правило «кто раньше».
- **Где пригодится:** «сначала самые дорогие/новые/крупные»; любое нестандартное упорядочивание.

> **Как подступиться.** Передайте третьим аргументом `[](int a, int b){ return a > b; }`.

Написал решение — прогони его на этих входах (нажми «копировать», вставь в программу), особенно на краях:

```tests
# один элемент; повторы
5 3 1 4 1 5 => 5 4 3 1 1
1 42 => 42
4 2 2 1 3 => 3 2 2 1
```

<details>
<summary>Подсказка посильнее — с чего начать</summary>

`std::sort` с лямбдой-компаратором `a > b` даёт убывание.

```c++
std::sort(v.begin(), v.end(), [](int a, int b){ return a > b; });
```

</details>

<details>
<summary>Полное решение с разбором — открывай, только если застрял</summary>

Ход мысли по шагам:

1. Читаю числа в `vector`.
2. Сортирую с собственным правилом — лямбдой `[](int a, int b){ return a > b; }` («больше идёт раньше»).
3. Печатаю через пробел.

```cpp
#include <iostream>
#include <vector>
#include <algorithm>

int main() {
    int n = 0;
    std::cin >> n;
    std::vector<int> v;
    for (int i = 0; i < n; ++i) {
        int x = 0;
        std::cin >> x;
        v.push_back(x);
    }

    std::sort(v.begin(), v.end(), [](int a, int b) { return a > b; });   // по убыванию

    bool first = true;
    for (int x : v) {
        if (!first) std::cout << " ";
        std::cout << x;
        first = false;
    }
    std::cout << "\n";
    return 0;
}
```

Открывай это, только когда правда застрял — весь смысл в том, чтобы пройти путь «затык → идея → код» самому.
</details>

---

## 7.3. Сколько больше среднего 🔴

*Сначала узнаём среднее по всем, потом считаем, кто выше. Среднее лямбда «прихватывает» с собой в `[avg]`.*

Дано `N` чисел. Напечатайте, сколько из них **строго больше** среднего
арифметического всей последовательности.

**Формат ввода.** `N` (1 ≤ N ≤ 100000), затем `N` целых чисел.

**Формат вывода.** Количество чисел, больших среднего.

**Пример:**
```
Ввод
5
1 2 3 4 100
Вывод
1
```

- **Тренирует:** два прохода (сначала среднее, потом счёт), `accumulate` + `count_if` с захватом.
- **Где пригодится:** статистика «выше среднего»: успеваемость, зарплаты, аномалии в данных.

> **Как подступиться.** Сначала посчитайте среднее по всем; потом посчитайте, сколько строго больше него. Среднее захватите в лямбду.

> **Разбор примера.** Среднее здесь `22`, больше него только `100`.

Написал решение — прогони его на этих входах (нажми «копировать», вставь в программу), особенно на краях:

```tests
# если все равны — никто не больше среднего
5 1 2 3 4 5 => 2
3 10 10 10 => 0
4 1 2 3 100 => 1
```

<details>
<summary>Подсказка посильнее — с чего начать</summary>

Сначала среднее по всем (сумму копи в `long long`), потом посчитай, сколько строго больше него.

```c++
long long sum = std::accumulate(v.begin(), v.end(), 0LL);
double avg = (double)sum / v.size();
auto count = std::count_if(v.begin(), v.end(), [avg](int x){ return x > avg; });
```

</details>

<details>
<summary>Полное решение с разбором — открывай, только если застрял</summary>

Ход мысли по шагам:

1. Сумму беру `std::accumulate(..., 0LL)` — `0LL` копит в `long long`.
2. Среднее дробное: `static_cast<double>(sum) / v.size()`.
3. Считаю подходящие через `count_if`, захватив среднее в лямбду `[avg]`; условие — строго `x > avg`.

```cpp
#include <iostream>
#include <vector>
#include <numeric>
#include <algorithm>

int main() {
    int n = 0;
    std::cin >> n;
    std::vector<int> v;
    for (int i = 0; i < n; ++i) {
        int x = 0;
        std::cin >> x;
        v.push_back(x);
    }

    long long sum = std::accumulate(v.begin(), v.end(), 0LL);   // 0LL — копим в long long
    double avg = static_cast<double>(sum) / static_cast<double>(v.size());

    auto count = std::count_if(v.begin(), v.end(),
                               [avg](int x) { return x > avg; });   // avg захвачено
    std::cout << count << "\n";
    return 0;
}
```

Открывай это, только когда правда застрял — весь смысл в том, чтобы пройти путь «затык → идея → код» самому.
</details>

---

## 7.4. Медиана 🟡

*Один выброс — и среднее врёт, а медиана держится. После сортировки она просто стоит в центре.*

Дано `N` чисел (`N` нечётное). Медиана — это число, оказавшееся ровно посередине,
если все числа отсортировать. Напечатайте медиану.

**Формат ввода.** Нечётное `N` (1 ≤ N ≤ 99999), затем `N` целых чисел.

**Формат вывода.** Медиана.

**Пример:**
```
Ввод
5
7 1 3 9 5
Вывод
5
```

- **Тренирует:** сортировку как подготовительный шаг и обращение по центральному индексу.
- **Где пригодится:** медиана устойчивее среднего к выбросам — её используют в статистике зарплат, времени отклика, цен.

> **Как подступиться.** Отсортируйте, возьмите элемент с индексом `N / 2` (для нечётного `N` это ровно середина).

Написал решение — прогони его на этих входах (нажми «копировать», вставь в программу), особенно на краях:

```tests
# один элемент; повторы
5 3 1 4 1 5 => 3
1 42 => 42
3 7 7 7 => 7
```

<details>
<summary>Подсказка посильнее — с чего начать</summary>

Отсортируй и возьми центральный элемент — при нечётном N это и есть медиана.

```c++
std::sort(v.begin(), v.end());
std::cout << v[v.size() / 2] << "\n";
```

</details>

<details>
<summary>Полное решение с разбором — открывай, только если застрял</summary>

Ход мысли по шагам:

1. Сортирую вектор.
2. Для нечётного `N` середина — индекс `N / 2` (целочисленное деление как раз даёт центр).
3. Печатаю `v[v.size() / 2]`.

```cpp
#include <iostream>
#include <vector>
#include <algorithm>

int main() {
    int n = 0;
    std::cin >> n;
    std::vector<int> v;
    for (int i = 0; i < n; ++i) {
        int x = 0;
        std::cin >> x;
        v.push_back(x);
    }

    std::sort(v.begin(), v.end());
    std::cout << v[v.size() / 2] << "\n";    // центр отсортированного
    return 0;
}
```

Открывай это, только когда правда застрял — весь смысл в том, чтобы пройти путь «затык → идея → код» самому.
</details>

---

## 7.5. Топ-3 по баллам 🔴

*Настоящий рейтинг сортируют по двум ключам сразу: по баллам, а при равенстве — по имени.*

Даны `N` участников: имя и баллы. Напечатайте имена трёх лучших по баллам. При
равенстве баллов выше тот, чьё имя меньше по алфавиту. Если участников меньше трёх —
напечатайте всех.

**Формат ввода.** `N` (1 ≤ N ≤ 100000), затем `N` пар: имя (латиница) и целое число баллов.

**Формат вывода.** До трёх строк — имена лучших, по одному в строке, в порядке убывания баллов.

**Пример:**
```
Ввод
4
anna 40
boris 55
vera 55
igor 20
Вывод
boris
vera
anna
```

- **Тренирует:** сортировку вектора структур по двум критериям сразу.
- **Где пригодится:** лидерборды, топ-N результатов поиска, рейтинги с тай-брейком.

> **Как подступиться.** Заведите `struct`, отсортируйте правилом «по баллам убыв., при равенстве по имени возр.», выведите первые три.

> **Разбор примера.** `boris` и `vera` по 55; `boris` меньше по алфавиту, поэтому он первый.

<details>
<summary>Подсказка посильнее — компаратор и «не больше трёх»</summary>

Правило сортировки задаётся лямбдой прямо в `std::sort`, а «первые три, но не больше, чем есть» — через `std::min`:

```c++
std::sort(v.begin(), v.end(), [](const P& a, const P& b){
    if (a.score != b.score) return a.score > b.score;  // балл по убыванию
    return a.name < b.name;                            // при равенстве — имя по алфавиту
});
std::size_t top = std::min<std::size_t>(3, v.size());  // если участников меньше трёх
```

</details>

<details>
<summary>Полное решение с разбором — открывай, только если застрял</summary>

Ход мысли по шагам:

1. Завожу `struct Participant { name; score; }` и читаю всех в `vector`.
2. Сортирую своим правилом: по баллам убыв., при равенстве — по имени возр.
3. Печатаю первые `min(3, размер)` имён.

```cpp
#include <iostream>
#include <vector>
#include <string>
#include <algorithm>

struct Participant {
    std::string name;
    int score = 0;
};

int main() {
    int n = 0;
    std::cin >> n;
    std::vector<Participant> people;
    for (int i = 0; i < n; ++i) {
        Participant p;
        std::cin >> p.name >> p.score;
        people.push_back(p);
    }

    std::sort(people.begin(), people.end(), [](const Participant &a, const Participant &b) {
        return a.score != b.score ? a.score > b.score : a.name < b.name;   // балл убыв., имя возр.
    });

    std::size_t top = std::min<std::size_t>(3, people.size());
    for (std::size_t i = 0; i < top; ++i)
        std::cout << people[i].name << "\n";
    return 0;
}
```

Открывай это, только когда правда застрял — весь смысл в том, чтобы пройти путь «затык → идея → код» самому.
</details>

---

## 7.6. Минимум и максимум одной строкой 🟢

*`min_element`/`max_element` найдут крайние значения без ручного цикла — только не забудьте `*`, иначе получите итератор.*

Дано `N` чисел. Напечатайте минимум и максимум, используя `std::min_element` и
`std::max_element`.

**Формат ввода.** `N` (1 ≤ N ≤ 100000), затем `N` целых чисел.

**Формат вывода.** Два числа через пробел: минимум и максимум.

**Пример:**
```
Ввод
5
3 1 4 1 5
Вывод
1 5
```

- **Тренирует:** готовые алгоритмы поиска и разыменование итератора (`*`).
- **Где пригодится:** границы диапазона, экстремумы — без ручного цикла.

> **Как подступиться.** `min_element` возвращает **итератор** (указатель на элемент); значение достаётся звёздочкой `*`.

Написал решение — прогони его на этих входах (нажми «копировать», вставь в программу), особенно на краях:

```tests
# один элемент; отрицательные
5 3 1 4 1 5 => 1 5
1 42 => 42 42
4 -5 0 5 2 => -5 5
```

<details>
<summary>Полное решение с разбором — открывай, только если застрял</summary>

Ход мысли по шагам:

1. Читаю числа в `vector`.
2. `std::min_element`/`std::max_element` возвращают **итераторы** — значение достаю звёздочкой `*`.
3. Печатаю минимум и максимум.

```cpp
#include <iostream>
#include <vector>
#include <algorithm>

int main() {
    int n = 0;
    std::cin >> n;
    std::vector<int> v;
    for (int i = 0; i < n; ++i) {
        int x = 0;
        std::cin >> x;
        v.push_back(x);
    }

    int mn = *std::min_element(v.begin(), v.end());   // * — достаём значение
    int mx = *std::max_element(v.begin(), v.end());
    std::cout << mn << " " << mx << "\n";
    return 0;
}
```

Открывай это, только когда правда застрял — весь смысл в том, чтобы пройти путь «затык → идея → код» самому.
</details>

---

## 7.7. Сколько чётных 🟡

*«Сколько подходящих?» — это `count_if`. Что считать подходящим, решает лямбда; здесь — чётность.*

Дано `N` чисел. Напечатайте, сколько среди них чётных, используя `std::count_if`.

**Формат ввода.** `N` (0 ≤ N ≤ 100000), затем `N` целых чисел.

**Формат вывода.** Количество чётных чисел.

**Пример:**
```
Ввод
5
1 2 3 4 6
Вывод
3
```

- **Тренирует:** `count_if` с лямбдой-условием.
- **Где пригодится:** «сколько подходящих» по любому признаку (активные пользователи, товары в наличии).

> **Как подступиться.** Правило чётности передайте лямбдой прямо в `count_if`.

Написал решение — прогони его на этих входах (нажми «копировать», вставь в программу), особенно на краях:

```tests
# нет чётных; и все чётные
6 1 2 3 4 5 6 => 3
3 1 3 5 => 0
4 2 4 6 8 => 4
```

<details>
<summary>Подсказка посильнее — с чего начать</summary>

`std::count_if` с предикатом чётности — одна строка.

```c++
auto count = std::count_if(v.begin(), v.end(), [](int x){ return x % 2 == 0; });
```

</details>

<details>
<summary>Полное решение с разбором — открывай, только если застрял</summary>

Ход мысли по шагам:

1. Читаю числа в `vector`.
2. `std::count_if` с лямбдой-условием `x % 2 == 0`.
3. Печатаю результат — это целое, можно выводить сразу.

```cpp
#include <iostream>
#include <vector>
#include <algorithm>

int main() {
    int n = 0;
    std::cin >> n;
    std::vector<int> v;
    for (int i = 0; i < n; ++i) {
        int x = 0;
        std::cin >> x;
        v.push_back(x);
    }

    auto count = std::count_if(v.begin(), v.end(), [](int x) { return x % 2 == 0; });
    std::cout << count << "\n";
    return 0;
}
```

Открывай это, только когда правда застрял — весь смысл в том, чтобы пройти путь «затык → идея → код» самому.
</details>

---

## 7.8. Слова по длине 🔴

*Сортируем не по самому значению, а по вычисленному ключу: сперва длина слова, а при равной длине — алфавит.*

Дано `N` слов. Напечатайте их отсортированными: сначала короткие, при равной длине —
по алфавиту. По одному слову в строке.

**Формат ввода.** `N` (1 ≤ N ≤ 100000), затем `N` слов (латиница).

**Формат вывода.** `N` строк — слова в нужном порядке.

**Пример:**
```
Ввод
4
banana kiwi fig apple
Вывод
fig
kiwi
apple
banana
```

- **Тренирует:** сортировку по вычисляемому ключу (длина) с тай-брейком по алфавиту.
- **Где пригодится:** ранжирование по нескольким критериям, «сначала короткие/дешёвые/новые».

> **Как подступиться.** Компаратор: если длины разные — сравниваем длины, иначе сравниваем сами строки.

<details>
<summary>Подсказка посильнее — с чего начать</summary>

`std::sort` с компаратором: разные длины — сравниваем длины, равные — по алфавиту.

```c++
std::sort(words.begin(), words.end(), [](const std::string &a, const std::string &b){
    if (a.size() != b.size()) return a.size() < b.size();
    return a < b;
});
```

</details>

<details>
<summary>Полное решение с разбором — открывай, только если застрял</summary>

Ход мысли по шагам:

1. Читаю слова в `vector<string>`.
2. Сортирую по вычисляемому ключу: если длины разные — по длине, иначе по алфавиту.
3. Печатаю по слову в строке.

```cpp
#include <iostream>
#include <vector>
#include <string>
#include <algorithm>

int main() {
    int n = 0;
    std::cin >> n;
    std::vector<std::string> words;
    for (int i = 0; i < n; ++i) {
        std::string w;
        std::cin >> w;
        words.push_back(w);
    }

    std::sort(words.begin(), words.end(), [](const std::string &a, const std::string &b) {
        if (a.size() != b.size()) return a.size() < b.size();   // сначала по длине
        return a < b;                                           // при равной — по алфавиту
    });

    for (const std::string &w : words)
        std::cout << w << "\n";
    return 0;
}
```

Открывай это, только когда правда застрял — весь смысл в том, чтобы пройти путь «затык → идея → код» самому.
</details>

---

# Подсказки

> **Мало подсказки?** Откройте теорию темы: [раздел справочника](../ref/07-algoritmy.md).

<details>
<summary>7.1 — По возрастанию</summary>

- Считайте в `std::vector<int> v`, затем `std::sort(v.begin(), v.end());` (из `<algorithm>`).
- Печатайте в цикле через пробел.
</details>

<details>
<summary>7.2 — По убыванию</summary>

- Та же сортировка, но с правилом: `std::sort(v.begin(), v.end(), [](int a, int b){ return a > b; });`.
- Лямбда `[](int a, int b){ return a > b; }` читается как «`a` должно идти раньше `b`, если оно больше».
- Альтернатива: отсортировать по возрастанию и печатать с конца.
</details>

<details>
<summary>7.3 — Больше среднего</summary>

- Сумму удобно взять через `std::accumulate(v.begin(), v.end(), 0LL)` (из `<numeric>`; `0LL` — чтобы копилось в `long long`).
- Среднее — дробное: `double avg = (double)sum / v.size();`.
- Посчитайте подходящие: `std::count_if(v.begin(), v.end(), [avg](int x){ return x > avg; });`. Обратите внимание: `avg` захвачено в `[avg]`, чтобы лямбда его видела.
</details>

<details>
<summary>7.4 — Медиана</summary>

- Отсортируйте вектор, ответ — элемент с индексом `N / 2` (целочисленное деление для нечётного `N` как раз даёт середину: при `N = 5` это индекс 2).
</details>

<details>
<summary>7.5 — Топ-3</summary>

- Заведите `struct P { std::string name; int score; };` и `std::vector<P>`.
- Сортируйте своим правилом: сначала по баллам убыванию, при равенстве — по имени возрастанию: `return a.score != b.score ? a.score > b.score : a.name < b.name;`.
- Напечатайте первые `std::min(3, (int)v.size())` имён.
</details>

<details>
<summary>7.6 — Минимум и максимум одной строкой</summary>

- `int mn = *std::min_element(v.begin(), v.end());` — не забудьте `*` (иначе получите итератор, а не число).
- Аналогично `*std::max_element(...)`.
</details>

<details>
<summary>7.7 — Сколько чётных</summary>

- `std::count_if(v.begin(), v.end(), [](int x){ return x % 2 == 0; });`.
- Результат — целое (тип `long long`/`ptrdiff_t`), его можно сразу печатать.
</details>

<details>
<summary>7.8 — Слова по длине</summary>

- Читайте в `std::vector<std::string>`.
- Компаратор: `if (a.size() != b.size()) return a.size() < b.size(); return a < b;`.
- Печатайте по слову в строке.
</details>

---

## Проверьте себя (мини тест-план)

- **7.1 / 7.2:** `N = 0` — пустой вывод; уже отсортированный массив; все одинаковые.
- **7.3:** все числа равны (никто не «больше среднего» → `0`); отрицательные числа.
- **7.4:** `N = 1` (медиана — само число); проверьте, что индекс именно `N / 2`.
- **7.5:** участников меньше трёх (вывести всех); тай-брейк по имени (равные баллы).
- **7.6:** `N = 1` (минимум равен максимуму); все одинаковые; отрицательные.
- **7.7:** ни одного чётного (`0`); все чётные; помните, что `0` — чётное.
- **7.8:** слова одинаковой длины (идут по алфавиту); уже отсортированный ввод.

**Тема закрыта,** когда вместо ручного цикла вы первым делом вспоминаете подходящий алгоритм из `<algorithm>`.

---

[← Контейнеры](06-konteynery.md) · [К списку тем](00-README.md) · [struct, математика, файлы →](08-struct-fayly.md)
