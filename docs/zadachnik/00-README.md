# Задачник по C++ — путь изучения

Это не просто список задач, а **маршрут**: 9 основных тем от типов до файлов плюс
бонусная 10-я, каждая опирается на предыдущую. В каждой теме — по 8 задач (в теме про
баги — 7, в «перепиши» — 6), от разминочных 🟢 до челленджей 🔴, с разбором «чему учит»
и «где пригодится», подсказками и мини тест-планом. **Готовых решений нет** — сверяешься
с теорией справочника и собственным тест-планом.

Идёшь по порядку — и к концу у тебя собирается рабочий набор навыков: посчитать,
проверить, перебрать, разложить на функции, сохранить в файл и починить чужой баг.

---

## Как устроен один шаг пути

Каждую тему проходи одним и тем же циклом — он и есть «путь изучения»:

```
1. ЧИТАЮ теорию      →  раздел справочника по теме (ссылки в таблице ниже)
2. СМОТРЮ пример     →  как приём выглядит в целой программе (examples/)
3. РЕШАЮ задачи      →  задачи темы, по нарастанию сложности (🟢 → 🔴)
4. ПРОВЕРЯЮ себя     →  мини тест-план в конце файла темы
5. ЧИНЮ баги         →  тема 9 (её бери параллельно, по мере сил)
```

Не пропускай шаг 1: задачи специально построены на ловушках, разобранных в
справочнике. Решать, не прочитав теорию, — как собирать мебель, не открыв инструкцию.

> 📖 **Справочник и примеры — в этом же окне.** Карта всего — [Начни отсюда](../00-НАЧНИ-ОТСЮДА.md),
> теория по темам — в группе «Справочник по темам», готовые программы — в «Примеры программ».
> Подсказки к задачам ведут прямо на нужный раздел справочника.

---

## Маршрут по темам

Темы идут по нарастанию. Ориентировочно — **одна тема в 2–4 дня**, по 1–2 задачи в день.

| # | Тема | Что освоишь | Теория (справочник) |
|---|---|---|---|
| 1 | [Основы](01-osnovy.md) | типы, `double` и точность, `char` как число, `const`, переполнение | разделы 1–3 |
| 2 | [Ввод и вывод](02-vvod-vyvod.md) | чтение чисел в цикле, до конца ввода, форматирование | раздел 4 |
| 3 | [Логика и циклы](03-logika-cikly.md) | `/` и `%`, ветвления, `switch`, вложенные циклы | разделы 5–8 |
| 4 | [Функции](04-funkcii.md) | свои функции, ссылки, возврат нескольких значений, рекурсия | раздел 9 |
| 5 | [Строки](05-stroki.md) | перебор символов, поиск, `substr`, разбор строки | раздел 10 |
| 6 | [Контейнеры](06-konteynery.md) | `vector`, `map`-счётчик, `set`, `pair` | разделы 11–15 |
| 7 | [Алгоритмы](07-algoritmy.md) | `sort`, `count_if`, лямбды, сортировка структур | разделы 17–18 |
| 8 | [struct, математика, файлы](08-struct-fayly.md) | свой `struct`, округление, чтение/запись файла | разделы 19–21 |
| 9 | [Ловим баги](09-lovim-bagi.md) | найти и исправить ошибку в готовой программе | разделы 11–12 |
| 10 | [Перепиши по-современному](11-perepishi.md) ⭐ | узнавать старый код и заменять на современный C++ | раздел 24 |

**Отметки сложности** в задачах: 🟢 всё нужное уже знаешь · 🟡 один новый приём · 🔴 надо подумать.

**⭐ Бонус** под задачей — необязательный поворот для тех, кому основного мало. Он не усложняет саму задачу: сначала базовое условие, звёздочка — на десерт.

Тема считается закрытой, когда можешь написать **три задачи блока подряд без подглядывания**.

> 🔎 **Нужна конкретная задача?** Открой [указатель задач](10-spisok-zadach.md) — все 77 задач
> одной таблицей, с быстрым выбором «размяться» (все 🟢) и «бросить вызов» (все 🔴).

---

## Как решать одну задачу (важно!)

Заведи под каждую задачу отдельный `.cpp` (например, `t1-1.cpp`) — так удобно
возвращаться к старым попыткам и сравнивать. Один заход — примерно 20–30 минут:

1. **Прочитал условие → закрыл файл.** Пиши по памяти, не подсматривая в пример.
2. **Собрал (`Ctrl+Alt+R`), добился нуля предупреждений.** Предупреждение — это тоже баг, просто ещё не сработавший.
3. **Прогнал пример из условия.** Совпало — переходи к тест-плану.
4. **Прогнал мини тест-план темы** (внизу каждого файла) — там «злые» входы: пустой, ноль, отрицательное, очень большое. **Именно на краях программы ломаются.**
5. **Сломалось → нашёл причину, а не просто починил.** Понял, к какому классу относится ошибка (см. тему 9).

**Порядок работы с подсказками** (не наоборот!):

1. Сначала решай сам — минимум 15 минут.
2. Застрял → раскрой **«Подсказку»** (наводка на идею, без кода).
3. Всё ещё не выходит → перейди по ссылке из подсказки в **теорию справочника** и разбери
   ловушку там. Готовых решений в задачнике нет намеренно — так задача действительно учит.

Застрял и после теории — покажи свой код и спроси, **на каком входе он ломается и почему**
(а не «дай ответ»). Разбор собственного кода полезнее любого готового.

---

## Универсальный тест-план

Прежде чем сказать «работает», прогони программу на этих входах — они ловят почти все типовые баги:

| Вход | Что проверяет |
|---|---|
| обычный пример из условия | базовая правильность |
| `0` | деление на ноль, пустой цикл, граница |
| отрицательное число | инициализация max/min, работа `%` |
| очень большое (`2000000000`, `10^18`) | переполнение `int`, выбор типа |
| буква вместо числа | сломанный `cin` (для задач с вводом) |
| пустой ввод / `N = 0` | цикл, который не должен выполниться |
| граничные значения (первый, последний, одинаковые) | ошибки «на единицу» |

---

## Отслеживание прогресса

Отмечай `[x]`, когда задача решена **и** прошла тест-план (а не просто «скомпилировалось»):

```
1  Основы            [ ][ ][ ][ ][ ][ ][ ][ ]
2  Ввод и вывод      [ ][ ][ ][ ][ ][ ][ ][ ]
3  Логика и циклы    [ ][ ][ ][ ][ ][ ][ ][ ]
4  Функции           [ ][ ][ ][ ][ ][ ][ ][ ]
5  Строки            [ ][ ][ ][ ][ ][ ][ ][ ]
6  Контейнеры        [ ][ ][ ][ ][ ][ ][ ][ ]
7  Алгоритмы         [ ][ ][ ][ ][ ][ ][ ][ ]
8  struct и файлы    [ ][ ][ ][ ][ ][ ][ ][ ]
9  Ловим баги        [ ][ ][ ][ ][ ][ ][ ]
10 Перепиши ⭐        [ ][ ][ ][ ][ ][ ]
```

### Звания

Считай решённые задачи — простой способ видеть, что растёшь:

| Решено | Звание |
|---|---|
| 10 | 🥉 Юниор — база на месте |
| 25 | 🥈 Уверенный — пишешь без подсказок |
| 45 | 🥇 Крепкий — ловушки не страшны |
| 60 | 💎 Продвинутый — читаешь и чинишь чужой код |
| все 77 | 🏆 Готов к большому задачнику ([08-zadachnik](../08-zadachnik.md)) |

**Норма — одна-две задачи в день.** Навык строится повторениями, а не объёмом за раз:
десять задач за выходные забудутся к среде, а по одной в день — останутся.

---

## Если застрял

Порядок действий, прежде чем спрашивать:

1. **Прочитал сообщение компилятора целиком?** Первую ошибку, не последнюю.
2. **Поставил `F9` и посмотрел значения в панели Variables?** Половина «непонятных» багов видна за десять секунд.
3. **Вывел промежуточные значения?** `std::cout << "x = " << x << "\n";` — грубо, но работает.
4. **Проверил на самом простом входе?** Не на `12345`, а на `5`.

Не помогло — спрашивай так, чтобы осталось чему научиться:

> «Вот задача 3.4 и мой код. Не переписывай — скажи, на каком входе он ломается и почему.»

Показывай свой код и проси объяснить **причину**, а не прислать готовый ответ. Иначе
научится тот, кто отвечает, а не ты.

---

## Что дальше

Когда закроешь все 10 тем — переходи к большому задачнику [08-zadachnik](../08-zadachnik.md)
(блоки A–K с мини-проектами: калькулятор-меню, крестики-нолики, банкомат). Он
устроен так же, но задачи крупнее и ближе к «настоящим» программам.

Решишь несколько и захочешь разбор или проверку кода — присылай, посмотрим вместе.
