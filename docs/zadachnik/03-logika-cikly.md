# Тема 3. Логика, ветвления, циклы

Здесь программа впервые начинает «принимать решения» и «повторять действия».
Ветвления (`if`, `switch`) и циклы — это скелет любой логики. Плюс ты закрепишь
пару `/` и `%`, на которой держится половина арифметических задач.

> 📖 **Опирается на разделы 5–8 справочника** ([теория в справочнике](../ref/03-logika-cikly.md)).
> Там разобраны ловушки чисел, `=` против `==`, забытый `break`, `<` против `<=`.

> 🔗 **В живых программах:** «Угадай число» (`../examples/04-ugaday-chislo.md`) — цикл до победы
> и ветвления «больше/меньше»; «Калькулятор» (`../examples/01-kalkulyator.md`) — выбор действия через `switch`.

[← К списку тем и пути изучения](00-README.md)

---

## 📋 Задачи темы

| № | | Задача | Что тренирует |
|---|---|---|---|
| 3.1 | 🟢 | Знак и чётность | `if/else`, `%` |
| 3.2 | 🟢 | Время года | `switch`, группы `case` |
| 3.3 | 🟡 | Сумма цифр | `% 10` и `/ 10` |
| 3.4 | 🟡 | Простое число? | перебор до `√n` |
| 3.5 | 🟡 | Треугольник | вложенный цикл |
| 3.6 | 🟢 | Максимум из трёх | цепочка сравнений |
| 3.7 | 🟡 | Количество делителей | делители парами |
| 3.8 | 🔴 | Ёлочка | два внутренних цикла |

<sub>🟢 знаешь всё нужное · 🟡 один новый приём · 🔴 надо подумать · [полный указатель задач](10-spisok-zadach.md)</sub>

---

## Чему ты здесь научишься

- Строить цепочки ветвлений `if / else if / else` и группировать `case` в `switch`.
- Раскладывать число на цифры циклом (`% 10`, `/ 10`).
- Писать вложенные циклы (цикл внутри цикла) и понимать, кто за что отвечает.
- Оптимизировать перебор (проверка простоты до корня, а не до `n`).

## Где это пригодится

Ветвление — это «в зависимости от…» в любой программе: роль пользователя, тип
файла, результат проверки. Циклы — это «для каждого…»: обработать все строки,
все пиксели, все дни. Разбор числа на цифры — классика собеседований и основа
контрольных сумм (проверка номера карты, ISBN, штрихкода).

---

## 3.1. Знак и чётность 🟢

Дано целое число. Напечатайте две характеристики: положительное оно,
отрицательное или ноль, и чётное оно или нечётное.

**Формат ввода.** Одно целое число, по модулю до 10^9.

**Формат вывода.** Два слова через пробел:
- первое — `POSITIVE`, `NEGATIVE` или `ZERO`;
- второе — `EVEN` или `ODD`.

**Пример 1:**
```
Ввод
-4
Вывод
NEGATIVE EVEN
```
**Пример 2:**
```
Ввод
0
Вывод
ZERO EVEN
```

**Что тренирует:** цепочка `if / else if / else`, проверка чётности через `%`.
**Где пригодится:** валидация ввода, разбиение на категории, «каждый второй элемент».
**Как подступиться:** знак и чётность — две **независимые** проверки, считай их отдельно.

---

## 3.2. Время года по месяцу 🟢

Дан номер месяца. Напечатайте, к какому времени года он относится. Решите
задачу через `switch`.

**Формат ввода.** Целое число от 1 до 12.

**Формат вывода.** Одно слово: `WINTER`, `SPRING`, `SUMMER` или `AUTUMN`.
Если число вне диапазона 1–12 — напечатайте `ERROR`.

**Пример 1:**
```
Ввод
1
Вывод
WINTER
```
**Пример 2:**
```
Ввод
10
Вывод
AUTUMN
```

**Что тренирует:** `switch` с группировкой `case` и обработкой «всё остальное» (`default`).
**Где пригодится:** меню, разбор кода команды, сопоставление «номер → категория».
**Как подступиться:** несколько `case` подряд без `break` — законный способ сказать «любой из этих».

---

## 3.3. Сумма цифр 🟡

Напечатайте сумму цифр неотрицательного целого числа.

**Формат ввода.** Одно неотрицательное целое число, не превосходящее 10^18.

**Формат вывода.** Сумма его цифр.

**Пример:**
```
Ввод
12345
Вывод
15
```

**Что тренирует:** извлечение цифр числа через `% 10` и `/ 10` в цикле.
**Где пригодится:** контрольные суммы (карты, штрихкоды), нумерология, разбор идентификаторов.
**Как подступиться:** последняя цифра — `n % 10`, «отрезать» её — `n /= 10`. Повторяй, пока `n > 0`.

> До 10^18 — намёк на выбор типа: `int` тут не подойдёт.

---

## 3.4. Простое число? 🟡

Определите, является ли число простым (делится только на 1 и на само себя,
и при этом больше 1).

**Формат ввода.** Одно целое число от 1 до 2 000 000 000.

**Формат вывода.** `YES`, если число простое, иначе `NO`.

**Пример 1:**
```
Ввод
7
Вывод
YES
```
**Пример 2:**
```
Ввод
1
Вывод
NO
```

**Что тренирует:** ранний выход из цикла, оптимизация перебора до `√n`.
**Где пригодится:** криптография (простые — её основа), хеш-таблицы, разложение на множители.
**Как подступиться:** не перебирай все делители до `n` — достаточно до корня. Подумай, почему, и как записать `i * i <= n` вместо `i <= sqrt(n)`.

> При больших `n` выражение `i * i` может переполнить `int` — держи `i` в `long long`
> или сравнивай `i <= n / i`.

---

## 3.5. Треугольник из звёздочек 🟡

Дана высота `n`. Напечатайте прямоугольный треугольник из символов `*`:
в первой строке одна звёздочка, во второй две, и так до `n`.

**Формат ввода.** Одно целое число `n` (1 ≤ n ≤ 50).

**Формат вывода.** `n` строк со звёздочками.

**Пример:**
```
Ввод
4
Вывод
*
**
***
****
```

**Что тренирует:** вложенный цикл — внешний по строкам, внутренний по символам.
**Где пригодится:** любой двумерный вывод: таблицы, сетки, ASCII-графика, поля игр.
**Как подступиться:** внешний цикл — строка `row` от 1 до `n`; внутренний печатает `row` звёздочек; после него — перевод строки.

---

## 3.6. Максимум из трёх 🟢

Даны три целых числа. Напечатайте наибольшее из них, **не используя** `std::max`.

**Формат ввода.** Три целых числа через пробел.

**Формат вывода.** Наибольшее из трёх.

**Пример:**
```
Ввод
3 7 5
Вывод
7
```

**Что тренирует:** цепочку сравнений и приём «текущий лучший».
**Где пригодится:** выбор лучшего варианта из нескольких (максимальная цена, ближайшая дата).
**Как подступиться:** заведи `best` первым числом и по очереди сравни его с остальными.

---

## 3.7. Количество делителей 🟡

Напечатайте, сколько у числа делителей (включая 1 и само число).

**Формат ввода.** Одно целое число `n` (1 ≤ n ≤ 10^9).

**Формат вывода.** Количество делителей.

**Пример:**
```
Ввод
12
Вывод
6
```

**Что тренирует:** перебор до корня и приём «делители ходят парами».
**Где пригодится:** теория чисел, разложение, оптимизация перебора вообще.
**Как подступиться:** каждый делитель `i ≤ √n` даёт парный `n / i`. Осторожно с точным квадратом — там пара совпадает.

> Делители 12: 1, 2, 3, 4, 6, 12 — шесть штук.

---

## 3.8. Ёлочка 🔴

Дана высота `n`. Напечатайте пирамиду из звёздочек по центру: в строке `i` сначала
идут пробелы, затем нечётное число звёздочек (1, 3, 5, …).

**Формат ввода.** Одно целое число `n` (1 ≤ n ≤ 50).

**Формат вывода.** `n` строк — пирамида, выровненная «домиком».

**Пример:**
```
Ввод
4
Вывод
   *
  ***
 *****
*******
```

**Что тренирует:** два цикла внутри одного — пробелы и звёздочки по отдельности.
**Где пригодится:** любой центрированный вывод и ASCII-графика.
**Как подступиться:** в строке `row` (от 1 до `n`) сначала `n − row` пробелов, потом `2·row − 1` звёздочек.

---

# Подсказки

> 📖 Мало подсказки? Открой теорию темы: [раздел справочника](../ref/03-logika-cikly.md).

<details>
<summary>3.1 — Знак и чётность</summary>

- Знак — обычная цепочка `if (n > 0) ... else if (n < 0) ... else ...`.
- Чётность — остаток от деления на 2: `n % 2 == 0` значит чётное.
- Осторожно: `%` с отрицательными числами может дать `-1` для нечётных. Надёжнее
  проверять `n % 2 == 0` (чётное), а всё остальное считать нечётным.
</details>

<details>
<summary>3.2 — Время года</summary>

- `switch (month)` и группируй `case`-ы: `case 12: case 1: case 2: std::cout << "WINTER"; break;`.
- Несколько `case` подряд без `break` — это законный способ сказать «любой из этих».
- Всё, что не 1–12, лови в `default: std::cout << "ERROR";`.
</details>

<details>
<summary>3.3 — Сумма цифр</summary>

- Тип: `long long n;` (10^18 в `int` не влезет).
- Последняя цифра — `n % 10`, отрезаем её через `n /= 10`.
- Цикл `while (n > 0) { sum += n % 10; n /= 10; }`. Для `n == 0` ответ `0` — проверь, что не сломается.
</details>

<details>
<summary>3.4 — Простое число</summary>

- Сразу отсеки `n <= 1` → `NO`.
- Перебирай делители от 2, пока `i * i <= n`. Если нашёлся делитель (`n % i == 0`) — число составное.
- `i * i` при больших `n` может переполнить `int` — держи `i` в `long long`, либо сравнивай `i <= n / i`.
- Дошёл до конца без делителей — число простое.
</details>

<details>
<summary>3.5 — Треугольник</summary>

- Внешний цикл по строкам: `for (int row = 1; row <= n; ++row)`.
- Внутренний — печатает `row` звёздочек: `for (int j = 0; j < row; ++j) std::cout << '*';`.
- После внутреннего цикла — перевод строки `std::cout << "\n";`.
</details>

<details>
<summary>3.6 — Максимум из трёх</summary>

- Заведи `int best = a;`, затем `if (b > best) best = b;` и `if (c > best) best = c;`.
- Приём «текущий лучший» масштабируется на любое количество чисел (в цикле).
</details>

<details>
<summary>3.7 — Количество делителей</summary>

- Перебирай `i` от 1, пока `i * i <= n` (держи `i` в `long long`).
- Если `n % i == 0` — это сразу два делителя: `i` и `n / i`, прибавляй `2`.
- Если `i == n / i` (точный квадрат), пара совпала — вычти один лишний.
</details>

<details>
<summary>3.8 — Ёлочка</summary>

- Внешний цикл: `for (int row = 1; row <= n; ++row)`.
- Первый внутренний печатает `n - row` пробелов, второй — `2 * row - 1` звёздочек.
- После двух внутренних циклов — перевод строки.
</details>

---

## Разбор убран намеренно

> 💡 **Готовые ответы здесь не лежат** — иначе задача перестаёт учить.
> Реши сам → застрял на 15+ минут → раскрой **подсказку** выше → всё ещё не выходит →
> покажи свой код и спроси **на каком входе он ломается и почему** (а не «дай готовый ответ»).
> Разбор собственного кода даёт больше, чем чужой готовый.

## Проверь себя (мини тест-план)

- **3.1:** `0` (должно быть `ZERO EVEN`); отрицательное нечётное (`-3` → `NEGATIVE ODD`).
- **3.2:** `0` и `13` — оба должны дать `ERROR`; граничные `2`, `3`, `11`, `12`.
- **3.3:** `0` → `0` (цикл `while (n > 0)` не выполнится — это верный ответ); максимальное 18-значное.
- **3.4:** `1` → `NO`, `2` → `YES`, большое простое (`1000000007` → `YES`).
- **3.5:** `n = 1` — ровно одна звёздочка.
- **3.6:** все равны (`5 5 5` → `5`); максимум в начале и в конце; отрицательные.
- **3.7:** `1` → `1`; простое число → `2`; точный квадрат (`16` → `5`) — проверь, что корень не посчитан дважды.
- **3.8:** `n = 1` — одна звёздочка без пробелов слева.

✅ Тема закрыта, когда вложенный цикл и `switch` пишешь без шпаргалки, а разбор числа на цифры — на автомате.

---

[← Ввод и вывод](02-vvod-vyvod.md) · [К списку тем](00-README.md) · [Функции →](04-funkcii.md)
