# 2. Список дел

> **Что показывает:** `vector` структур, нумерация для человека, удаление, `std::erase_if`.
> **Готовый файл:** [code/todo.cpp](code/todo.cpp) — открой и нажми `Ctrl+Alt+R`.
> Собирается без единого предупреждения со всеми строгими флагами.

[← Все примеры](README.md) · [← Калькулятор](01-kalkulyator.md) · [Анализ текста →](03-analiz-teksta.md) · [Начни отсюда](../00-НАЧНИ-ОТСЮДА.md)

---

## Код целиком

```cpp
// Список дел: добавить, отметить выполненным, удалить, посмотреть.
// Показывает: vector структур, поиск по номеру, удаление, подсчёт, std::erase_if.

#include <windows.h>

#include <cstdlib>           // std::exit
#include <format>
#include <iostream>
#include <limits>
#include <string>
#include <vector>

struct Task {
  std::string title;      // что сделать
  bool done = false;      // выполнено или нет
};

int readInt(const std::string &prompt) {
  int value = 0;
  while (true) {
    std::cout << prompt;
    if (std::cin >> value) {
      std::cin.ignore(std::numeric_limits<std::streamsize>::max(), '\n');
      return value;
    }
    if (std::cin.eof()) {                    // ввод кончился (Ctrl+Z или конец файла)
      std::cout << "\nВвод закончился.\n";   // без неё цикл крутился бы вечно
      std::exit(0);
    }
    std::cin.clear();
    std::cin.ignore(std::numeric_limits<std::streamsize>::max(), '\n');
    std::cout << "Нужно число.\n";
  }
}

std::string readLine(const std::string &prompt) {
  std::cout << prompt;
  std::string line;
  if (!std::getline(std::cin, line)) {     // ввод кончился — дальше спрашивать нечего
    std::cout << "\nВвод закончился.\n";
    std::exit(0);
  }
  return line;
}

// Показывает список с номерами, начиная с 1 — так привычнее человеку,
// хотя внутри вектора индексы начинаются с 0.
void printTasks(const std::vector<Task> &tasks) {
  if (tasks.empty()) {                       // пустой список — отдельный случай,
    std::cout << "Список пуст.\n";           // иначе человек видит просто ничего и теряется
    return;
  }
  std::cout << "\n--- Дела ---\n";
  for (std::size_t i = 0; i < tasks.size(); ++i) {
    const char *mark = tasks[i].done ? "[x]" : "[ ]";     // галочка или пусто
    std::cout << std::format("{}. {} {}\n", i + 1, mark, tasks[i].title);
  }
  // Считаем выполненные обычным циклом — заодно видно, сколько осталось
  int doneCount = 0;
  for (const auto &t : tasks)
    if (t.done)
      ++doneCount;
  std::cout << std::format("Выполнено {} из {}\n", doneCount, tasks.size());
}

// Возвращает индекс в векторе или -1, если номер неверный.
// Отдельная функция, потому что проверка нужна и при отметке, и при удалении.
int askTaskIndex(const std::vector<Task> &tasks, const std::string &prompt) {
  if (tasks.empty()) {
    std::cout << "Сначала добавьте хотя бы одно дело.\n";
    return -1;
  }
  int number = readInt(prompt);                    // человек вводит 1, 2, 3...
  if (number < 1 || number > static_cast<int>(tasks.size())) {
    std::cout << "Нет дела с таким номером.\n";    // ✅ проверка ДО обращения к вектору
    return -1;
  }
  return number - 1;                               // ...а вектору нужен индекс на 1 меньше
}

int main() {
  SetConsoleCP(CP_UTF8);
  SetConsoleOutputCP(CP_UTF8);

  std::vector<Task> tasks;
  bool running = true;

  while (running) {
    std::cout << "\n1 - показать  2 - добавить  3 - отметить  4 - удалить  "
                 "5 - убрать выполненные  0 - выход\n";
    switch (readInt("Выбор: ")) {
    case 1:
      printTasks(tasks);
      break;

    case 2: {
      std::string title = readLine("Что сделать: ");
      if (title.empty()) {                        // пустое дело добавлять бессмысленно
        std::cout << "Пустое название, отменено.\n";
        break;
      }
      tasks.push_back({title, false});            // собираем Task прямо в push_back
      std::cout << "Добавлено.\n";
      break;
    }

    case 3: {
      int index = askTaskIndex(tasks, "Номер выполненного: ");
      if (index >= 0) {
        tasks[static_cast<std::size_t>(index)].done = true;
        std::cout << "Отмечено.\n";
      }
      break;
    }

    case 4: {
      int index = askTaskIndex(tasks, "Номер для удаления: ");
      if (index >= 0) {
        // erase принимает итератор, поэтому к началу прибавляем номер
        tasks.erase(tasks.begin() + index);
        std::cout << "Удалено.\n";
      }
      break;
    }

    case 5: {
      // Убрать все выполненные разом — одна строка вместо цикла с erase
      std::size_t removed = std::erase_if(tasks, [](const Task &t) { return t.done; });
      std::cout << std::format("Убрано выполненных: {}\n", removed);
      break;
    }

    case 0:
      running = false;
      std::cout << "Пока!\n";
      break;

    default:
      std::cout << "Нет такого пункта.\n";
      break;
    }
  }
  return 0;
}
```

## Что происходит при запуске

_Добавили два дела, отметили одно, убрали выполненные_

```

1 - показать  2 - добавить  3 - отметить  4 - удалить  5 - убрать выполненные  0 - выход
Выбор: Что сделать: Добавлено.

1 - показать  2 - добавить  3 - отметить  4 - удалить  5 - убрать выполненные  0 - выход
Выбор: Что сделать: Добавлено.

1 - показать  2 - добавить  3 - отметить  4 - удалить  5 - убрать выполненные  0 - выход
Выбор: 
--- Дела ---
1. [ ] Купить хлеб
2. [ ] Сделать ДЗ
Выполнено 0 из 2

1 - показать  2 - добавить  3 - отметить  4 - удалить  5 - убрать выполненные  0 - выход
Выбор: Номер выполненного: Отмечено.

1 - показать  2 - добавить  3 - отметить  4 - удалить  5 - убрать выполненные  0 - выход
Выбор: 
--- Дела ---
1. [ ] Купить хлеб
2. [x] Сделать ДЗ
Выполнено 1 из 2

1 - показать  2 - добавить  3 - отметить  4 - удалить  5 - убрать выполненные  0 - выход
Выбор: Убрано выполненных: 1

1 - показать  2 - добавить  3 - отметить  4 - удалить  5 - убрать выполненные  0 - выход
Выбор: 
--- Дела ---
1. [ ] Купить хлеб
Выполнено 0 из 1

1 - показать  2 - добавить  3 - отметить  4 - удалить  5 - убрать выполненные  0 - выход
Выбор: Пока!
```

## Разбор: почему сделано именно так

**Номера для человека начинаются с 1, а индексы вектора — с 0.** Перевод делается в одном месте — в `askTaskIndex` (`return number - 1;`). Если разбросать `-1` по всей программе, рано или поздно где-то забудешь и получишь сдвиг на единицу.

**`askTaskIndex` возвращает `-1` как признак «не годится».** Это тот самый случай, где уместнее `std::optional<std::size_t>` (см. [раздел 9](../ref/04-funkcii.md#9-функции) в [Функциях](../ref/04-funkcii.md)). Здесь оставлен `-1`, чтобы программа читалась совсем просто, но попробуй переписать её на `optional` — это хорошее упражнение.

**Проверка `tasks.empty()` стоит перед вопросом о номере.** Иначе программа спросила бы «номер дела» у человека, у которого дел нет, — и это выглядело бы издевательством.

**`std::erase_if` убирает выполненные одной строкой.** Ручной цикл с `erase` здесь опасен: после удаления элементы сдвигаются, и следующий индекс перескакивает через один. `erase_if` эту ловушку убирает целиком.

**`printTasks` считает выполненные при каждом показе, а не хранит счётчик.** Хранимый счётчик пришлось бы обновлять в четырёх местах и он бы рассинхронизировался. Посчитать заново по списку из десяти дел — бесплатно.

**`const char *mark = ...` вместо `std::string`.** Здесь строка не меняется и не хранится — обычный указатель на литерал подходит и не создаёт лишнего объекта. Если непонятно, чем это отличается от `std::string`, просто напиши `std::string mark` — работать будет так же.

## Что попробовать изменить

1. Добавь приоритет (`int priority`) и показывай сначала важные — понадобится `std::sort` с лямбдой.
2. Сохраняй список в файл при выходе и загружай при старте — как в [Телефонной книге](06-telefonnaya-kniga.md).
3. Добавь пункт «снять отметку» — почти копия «отметить», но `done = false`.
4. Покажи, сколько дел добавлено сегодня: заведи поле с датой (строкой) и считай по нему.
5. Сложное: добавь редактирование названия. Подумай, как быть, если пользователь ввёл пустую строку — оставить старое или считать это отменой?

---

[← Все примеры](README.md) · [← Калькулятор](01-kalkulyator.md) · [Анализ текста →](03-analiz-teksta.md) · [Начни отсюда](../00-НАЧНИ-ОТСЮДА.md)
