# Документация C++ — панель

Локальное расширение: список всех материалов проекта — справочник, примеры, инструменты.

- клик по строке — открыть файл (по умолчанию в режиме чтения); правый клик — открыть как текст;
- поиск сверху ищет по названию, описанию и тексту файлов, без учёта регистра и ё/е;
- у каждого материала видно время чтения (`~N мин`) и число разделов;
- в широком окне (отдельная панель) материалы раскладываются в несколько колонок автоматически;
- отметки «изучено» (○/✓, при наведении), полоса прогресса «изучено N из M · %» и кнопка **▶ Продолжить** — к следующему неизученному;
- клавиатура: `↑`/`↓` и `PageUp`/`PageDown` — выбор, `Enter` — открыть, `/` или `Ctrl+K` — в поиск;
- кнопки внизу запускают задачи «прогнать тесты» и «проверить документацию».

## Плавающее окно (v3)

Основной способ читать — **плавающее окно поверх редактора**: перетаскивается за заголовок,
тянется за края, запоминает позицию. Слева навигатор по всем материалам (группы, поиск,
прогресс, закладки), справа — текст с **подсветкой C++**, кнопками «копировать код», таблицами,
оглавлением и переходами по внутренним ссылкам. Тема берётся из VS Code.

Окно работает через загрузчик **Custom UI Style** (`subframe7536.custom-ui-style`) или
**Custom CSS and JS** (`be5invis.vscode-custom-css`) — как MoonLight custom-bg. Подключить:
команда **«Документация C++: подключить плавающее окно»** (или кнопка `▣` в шапке панели) →
«Применить». Затем нажми пилюлю **«📘 C++»** справа внизу редактора.

## Где открыть меню-панель

Кроме окна остаётся прежнее меню — двумя способами:

- **Отдельной панелью** (во всю ширину, в области редактора) — кнопка **«C++»** в статус-баре
  внизу, кнопка **↗** в шапке вьюхи в сайдбаре, или команда **«Документация C++: открыть меню
  отдельной панелью»** из палитры (`Ctrl+Shift+P`). Удобно, когда в узком сайдбаре тесно.
- **В левом сайдбаре** — иконка книги в панели действий (как раньше).

Оба места показывают одинаковый список и синхронизированы: закладки, «недавнее» и подсветка
открытого файла общие.

Названия и описания берутся из самих файлов (первый заголовок и первая строка цитаты),
поэтому новые файлы появляются в списке сами.

## Настройки

| Настройка | Смысл |
|---|---|
| `cppDocs.path` | где лежит документация, если проект открыт не из своей папки |
| `cppDocs.openInPreview` | `true` — открывать в режиме чтения, `false` — как текст |

## Удалить

`Ctrl+Shift+X` → найти «Документация C++» → Uninstall.
